// Placeholder. Generate the real file after applying migrations:
//   supabase gen types typescript --project-id <PROJECT_REF> --schema public \
//     > supabase/types/database.types.ts
// Until then, this file exports a minimal stub so the app compiles.
export type Database = Record<string, unknown>;
